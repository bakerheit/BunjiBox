{
  // Node-only runtime and stores. Scripts are checked with the same strict
  // profile because they drive the real modules.
  "extends": "../../tsconfig.base.json",
  "include": ["src", "scripts"]
}
