{
  "name": "@bunji/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared design tokens. Import after `tailwindcss` in an app's own CSS entry so Tailwind source detection stays rooted in that app.",
  "exports": {
    "./theme.css": "./theme.css"
  }
}
